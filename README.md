# Adressable_DRC_extension
Adressable version of the DRC protocol(DRC is originally done by Gordon Henderson)

TODO 
  upload a pythonscript that allows controlling more than one arduino.
  more documentation
